# 4.4: Evaluation criteria publish (staff portal visibility)

**Story ID:** 4.4
**Epic:** 4 — Audit & Compliance
**Slug:** evaluation-criteria-publish
**Status:** backlog

## Story

As a staff member,
I want to see the evaluation grid and scoring criteria in my staff portal,
so that I understand how my performance will be assessed before and after evaluation, per PA transparency law.

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. The staff portal exposes a read-only "Evaluation criteria" view listing the current evaluation grid — criteria names, weights/scoring bands, and descriptions — for the authenticated staff member's tenant. [Source: prd.md#NFR-REG-001 — "Evaluation criteria public: publish evaluation grid in staff portal"]
2. The view is plain Blade, consistent with the existing staff portal pattern established in 3.6 (no Filament, no edit affordances). [Source: architecture.md#adr-002 — Filament admin + thin Blade staff portal] [Source: laravel/Themes/One/docs/stories/3.6.staff-portal-readonly-view.story.md]
3. The view is reachable only by an authenticated staff member and scoped to their own tenant's criteria set, reusing 3.6's auth/tenant-scoping pattern. [Source: prd.md#NFR-SEC-002] [Source: laravel/Themes/One/docs/stories/3.6.staff-portal-readonly-view.story.md]
4. If a tenant has not yet configured an evaluation grid, the view renders a clear "not yet published" state rather than an error or an empty table. [Inference — UX completeness requirement synthesized from the general staff-portal read-only pattern; not separately specified in prd.md]
5. The evaluation grid content displayed matches the same criteria set used to compute scores, so what staff see here is provably the same data 4.5's transparent-scoring view maps scores against. [Source: prd.md#NFR-REG-001] [Inference — consistency constraint linking 4.4 and 4.5's data source]

## Tasks / Subtasks

- [ ] Confirm the existing evaluation-criteria data source (`CriteriValutazione` model, per Progressioni module) and read from it without modification (AC: #1, #5)
- [ ] Add a new controller action (e.g. `Themes/One/app/Http/Controllers/EvaluationCriteriaController.php`) rendering the criteria list, distinct from `StaffPortalController.php` (AC: #1, #2)
- [ ] Add the Blade view `Themes/One/resources/views/staff-portal/evaluation-criteria.blade.php` following 3.6's layout conventions (AC: #2)
- [ ] Apply the existing staff-portal auth middleware/policy and tenant scope to the new route (AC: #3)
- [ ] Render an explicit "not yet published" empty state when no criteria exist for the tenant (AC: #4)
- [ ] Add a navigation link from the existing staff portal entry page (3.6's view) to this new criteria view, additive only (AC: #1)
- [ ] Write feature tests covering visibility, scoping, and empty state (AC: #1-#5)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

- **NFR-REG-001 full text**: "Evaluation criteria public: publish evaluation grid in staff portal. Transparent scoring: staff see evaluation scores + criteria mapping. Appeal logging: log all appeals in Activity table with resolution. Documentation: archive evaluation policy + scoring matrix with each PA deployment." [Source: prd.md#NFR-REG-001] This story owns only the "Evaluation criteria public" bullet; "Transparent scoring" is 4.5, "Appeal logging" is 4.3.
- **ADR-002 grounding**: the staff portal is deliberately "thin Blade" — no Filament — to keep the staff-facing surface simple and auditable; this story must not introduce a Filament resource for staff-facing criteria display. [Source: architecture.md#adr-002]
- **3.6 pattern reuse**: story 3.6 ("Staff portal read-only view") already established `StaffPortalController.php` and the staff-portal Blade layout, auth, and tenant-scoping pattern. [Source: laravel/Themes/One/docs/stories/3.6.staff-portal-readonly-view.story.md] Per sharding-context.md, `Themes/One/app/Http/Controllers/StaffPortalController.php` is owned exclusively by Epic 3/story 3.6 — this story must add a NEW controller rather than modify that file. [Source: sharding-context.md#owned-file-module-scope — Epic 3 row]
- **Existing criteria model located**: `Modules/Progressioni/app/Models/CriteriValutazione.php` exists in the codebase already (confirmed by directory listing) — this story reads from it, it does not define the criteria schema itself. [Inference — confirmed by reading `laravel/Modules/Progressioni/app/Models/` listing]
- **Scoring/document component boundary**: architecture.md's component design places "Evaluation criteria" and "scoring" under the Scheda/Progressioni domain (Rating/Progressioni components), while the Audit/GDPR component only covers Activity/export. This story's job is purely a presentation-layer read view in Themes/One over existing Progressioni data — it does not create or modify evaluation-criteria domain logic. [Inference — boundary inferred from architecture.md's component-responsibility table, not a direct quote]
- **Coverage matrix**: NFR-REG-001 does not appear as its own row in the FR/NFR Coverage Matrix excerpt reviewed for this epic; its constituent capabilities ride on the Scheda Management and Access Control components already covering criteria/evaluation display. [Inference — matrix gap noted for consistency with 4.3's Dev Notes]

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- **Test type:** Feature tests (Pest) against the new route, reusing 3.6's staff-auth test fixtures/pattern.
- **Fixtures:** `User` factory (staff role, tenant-scoped, per 3.6's convention); `CriteriValutazione` factory/seeded rows for one tenant; a second tenant with no criteria configured, to test the empty state.
- **Scenarios to prove each AC:**
  - AC #1: authenticated staff user sees all criteria rows (name, weight/band, description) for their tenant.
  - AC #2: assert the response is a Blade view (not a Filament page/route) and contains no edit/save form controls.
  - AC #3: an unauthenticated request is redirected to login; a staff user from tenant A cannot see tenant B's criteria (cross-tenant assertion, mirroring 3.6's tenant-scope test).
  - AC #4: a tenant with zero `CriteriValutazione` rows renders the "not yet published" message, not a 500 or an empty table shell.
  - AC #5: assert the criteria list rendered here matches (by ID/name) the criteria set 4.5's scoring view references, using a shared fixture set between the two stories' tests where practical.
- **Edge cases:** a criteria set mid-update (some rows saved, some not) — this story should render whatever the current committed state is, no partial-save handling needed since this is read-only; very large criteria lists (pagination not required by any cited source, but note the assumption in the implementation if the list is rendered unpaginated).

## Dependency Maps

- Blocked by: 3.6 (staff portal auth/layout pattern must exist before this view is added), 3.5 (tenant isolation)
- Blocks: 4.5 (transparent scoring view maps scores against this story's published criteria)
- External: `Modules/Progressioni/app/Models/CriteriValutazione.php` (read-only dependency, not modified)

## Owned File/Module Scope

- `Themes/One/app/Http/Controllers/EvaluationCriteriaController.php` — new
- `Themes/One/resources/views/staff-portal/evaluation-criteria.blade.php` — new
- `Themes/One/tests/Feature/StaffPortal/EvaluationCriteriaTest.php` — new
- `Themes/One/resources/views/staff-portal/index.blade.php` (or 3.6's equivalent entry view) — additive-only edit to add a navigation link; do not restructure existing content
- Explicitly NOT owned: `Themes/One/app/Http/Controllers/StaffPortalController.php` (Epic 3/3.6-owned); `Modules/Progressioni/app/Models/CriteriValutazione.php` (read-only, not modified)

## Learnings from Previous Stories

- 3.6 established the staff-portal auth middleware, tenant-scoping pattern, and Blade-only layout convention — reused directly here rather than re-derived. (from 3.6)
- Sibling stories in Epic 3/4 consistently avoid modifying another story's exclusively-owned controller, instead adding a new, narrowly-scoped controller and linking to it — applied here with `EvaluationCriteriaController.php`. (from 3.7, 4.3)

## Dev Agent Record

_(empty — to be completed by the external dev tool)_
